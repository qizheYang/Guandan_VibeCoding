name: guandan
description: "掼蛋 - Online multiplayer card game"
publish_to: 'none'
version: 1.0.5+5

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.3
  provider: ^6.1.2
  flutter_svg: ^2.0.17
  guandan_shared:
    path: shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/cards/
